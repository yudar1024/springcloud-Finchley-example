spring:
  application:
    name: myapp

server:
  port: 8090

debug: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"




